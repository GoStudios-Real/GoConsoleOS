<UserControl x:Class="GoConsoleOS.GoConsole.Views.DownloadManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="DOWNLOADS" FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,8" />
        <TextBlock Grid.Row="1" x:Name="DownloadStatus" Text="0 active downloads"
                   FontSize="14" Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="2" Style="{StaticResource InvisibleScrollViewer}">
            <ItemsControl x:Name="DownloadList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12" Background="{StaticResource BrushBackgroundLight}"
                                Padding="16" Margin="0,0,0,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Name}"
                                           FontSize="15" FontWeight="Bold"
                                           Foreground="{StaticResource BrushTextPrimary}" />
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Status}"
                                           FontSize="12" FontWeight="Bold"
                                           Foreground="{Binding StatusColor}"
                                           VerticalAlignment="Center" />

                                <ProgressBar Grid.Row="1" Grid.ColumnSpan="2"
                                             Value="{Binding ProgressPercent}" Minimum="0" Maximum="100"
                                             Height="6" Margin="0,8"
                                             Foreground="{StaticResource BrushAccentPrimary}"
                                             Background="{StaticResource BrushBackgroundCard}"
                                             BorderThickness="0" />

                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal">
                                    <TextBlock Text="{Binding ProgressText}" FontSize="11"
                                               Foreground="{StaticResource BrushTextSecondary}"
                                               VerticalAlignment="Center" Margin="0,0,12,0" />
                                    <Border CornerRadius="4" Background="{StaticResource BrushBackgroundCard}"
                                            Padding="8,3" Cursor="Hand"
                                            MouseLeftButtonDown="TogglePause" Tag="{Binding Id}">
                                        <TextBlock Text="{Binding ActionText}" FontSize="10" FontWeight="Bold"
                                                   Foreground="{StaticResource BrushAccentPrimary}" />
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
