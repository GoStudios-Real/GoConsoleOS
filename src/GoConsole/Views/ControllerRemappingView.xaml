<UserControl x:Class="GoConsoleOS.GoConsole.Views.ControllerRemappingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CONTROLLER REMAPPING" FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,8" />
        <TextBlock Grid.Row="1" Text="Reassign controller buttons. Changes apply globally."
                   FontSize="14" Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="2" Style="{StaticResource InvisibleScrollViewer}">
            <ItemsControl x:Name="RemapGrid">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="200" Margin="6" CornerRadius="12"
                                Background="{StaticResource BrushBackgroundLight}" Padding="16">
                            <StackPanel>
                                <TextBlock Text="{Binding Button}" FontSize="14" FontWeight="Bold"
                                           Foreground="{StaticResource BrushAccentPrimary}" />
                                <TextBlock Text="→" FontSize="20"
                                           Foreground="{StaticResource BrushTextMuted}"
                                           HorizontalAlignment="Center" Margin="0,8" />
                                <Border CornerRadius="6" Background="{StaticResource BrushBackgroundCard}"
                                        Padding="8,6" Cursor="Hand"
                                        MouseLeftButtonDown="RemapButton" Tag="{Binding Id}">
                                    <TextBlock x:Name="MappedToText" Text="{Binding MappedTo}"
                                               FontSize="13" FontWeight="Bold"
                                               Foreground="{StaticResource BrushTextPrimary}"
                                               HorizontalAlignment="Center" />
                                </Border>
                                <Border CornerRadius="4" Background="{StaticResource BrushBackgroundCardHover}"
                                        Padding="6,3" Margin="0,6,0,0" Cursor="Hand"
                                        MouseLeftButtonDown="ResetButton" Tag="{Binding Id}">
                                    <TextBlock Text="RESET" FontSize="9" FontWeight="Bold"
                                               Foreground="{StaticResource BrushTextMuted}"
                                               HorizontalAlignment="Center" />
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
